services:
  - type: web
    name: docforge-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: mkdir -p temp && uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: RENDER_EXTERNAL_URL
        value: https://docforge-backend.onrender.com
